**오늘의 한 줄 평:** React Query, TanStack Query 에 대해 배웠다. 
- 오후에는 미니 프로젝트 시작 회의를 했다. 나는 평소에 노래 듣는것을 좋아해 아이디어를 내 제안 했고 간단한 노래 앱을 만들기로 했다. 내가 좋아하는 것을 만들고 결과물이 만들다니 동기부여가 돼었다. 만들 대상이 정해지고 화면과 담당 화면/기능을 정하고, git 초기 세팅을 했다.
- 메인화면에서 플레이리스트를 보여주기 위해 간단하게 사용 할 수 있는 last.fm의 search API를 key를 받았는데, 적용하려고 보니 검색 API이기 때문에 키워드에 해당하는 노래 목록을 불러오고 여러가지 노래 플레이리스트를 표시하기 어려워 Spotify playlist API를 사용해보려 했다. 그런데 token 받기 에러를 풀지 못헀다. db에 데이터를 넣고 불러오는 방식으로 바꿨다.
- 협업용 git 세팅도 처음 해봤다. 앞으로 잘 숙지해야겠다.
이후에는 플젝 명, 핵심 기능, Zutand Store 목록, API 데이터 구조, 와이어프레임을 작성했다.


#### 꿀팁
- 

2026-04-24 수업 15일차
==========
### 전날 02_zustand-and-query 예제 복습
- 공용 store를 만드는 명령어 : create. 
- 상태 변경 함수 : set state. 
  - 선언 시, 함수안에서 객체 형태({key:value})로 선언
-코드 예시
```
export const useUserStore = create( (set) => ({
    user: null,
    loading: false,
    
    //Three states
    login: (userData) => set( {user: userData}),
    logout: () => set( {user:null} ),
    setUser: (user) => set({user}),
    login: (userData) => set( {user: userData}), //login : 로그인이라는 함수 선언 하고나면, userData 값으로 user 값 변경
...
```
- 상황에 따라 전역 창고 store를 쓰거나, usestae를 쓴다.
- store 이름 앞에 꼭 use 사용. 가운데 기능. 끝에는 Store
    - useCarStore.js : 
    - useUIStore.js : 화면 그리는 store
- zu stand git 다운 시, npm install 필수

### 오늘 02_zustand-and-query 수업
- usePersistStore.js : 새로고침 시 상태 고정
- persist :localStorage에 저장하는 기능. persist에 값 전달. localStorge는 브라우저가 제공하는 공간으로, 브라우저에 데이터 저장하게 한다. 브라우저를 껐다 켜도 데이터가 저장되어 있다. 대신 보안이 취약에서 민감한 정보는 저장하지 않는다.
- persist/page.js : persist에 왔을때 렌더링 될 페이지 생성

### React Query(TanStack Query)
- 코드가 섞이지 않고 가독성 높이는 쿼리
- 명령어: npm install @tanstack/react-query 
- fetch, 로딩, 서버 상태 관리 자동 라이브러리
- app/Providers.js : 앱 전체에서 데이터 공유 공급소. props로 공급하던 데이터를 provider로 리액트에서 제공해주는 query client를 provider로 제공. 반드시 client component.
- app/api/userApi.js
- staleTime: 60 * 1000, //1분동안은 다시 요청하지 않고, 캐시를 사용. 데이터는 몇 ms 동안 최신 데이터라고 믿을래? =>성능 좋아짐 
- layout.js
```
export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>
        <Providers>
          {children}
        </Providers>
        </body>        
    </html>
  );
}
```
- 서버 상태 = React Query. 클라이언트(UI) 상태 = useState / Zustand 사용
   - /query/page.js 
   - 유저 목록 불러오기
```
서버에서 가져오는 데이터(게시글 목록, 유저 정보, 상품 목록 등) 처럼
“서버와 동기화가 필요한 데이터”는 TanStack Query(React Query) 로 관리하는 게 좋다.
이유는 TanStack Query가 캐싱, 자동 재요청, 로딩/에러 상태 관리, refetch 같은 기능을 기본으로 제공하기 때문.

📌 반대로,

UI 상태(모달 열림/닫힘, 다크모드, 입력값 등) 처럼
서버랑 상관없는 데이터는 useState나 Zustand 같은 상태관리로 처리한다
```

### 미니 프로젝트 Tip
#### db.json 
- 최상위 폴더에 db.json 생성
- npx json-server db.json --port 3001
- Endpoint에 3001/posts 생성됨
```
Endpoints:
http://localhost:3001/posts
```
- CRUD
    - 생성 : POST
    - 수정 : PUT, PATCH
    - 삭제 : DELETE

### git 관리
- git OR 생성 > repo 생성 > 플젝 생성 > git remote > 환경 변수 파일 생성 > git add . > git commit -m "메세지" > git push main origin > 
- branch 따기 : git checkout feature/[기능 이름]
- 받아오기 : git pull
- git 상태 : git status. git commit 전 꼭 git 상태 확인
- git restore . : 커밋 안된 수정 파일을 최종 상태로 되돌림