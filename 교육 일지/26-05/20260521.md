**오늘의 한 줄 평:**  Next > Servelet > servcie > DAO > JDBC > MYSQL > JSON 응답 프로젝트의 흐름을 이해했다. (간신히) 코드를 쓸때는 이해가 안가서 page.js의 버튼 클릭부터 시작해서 흐름을 이해했고, 가다가 모르는건 재미나이에 물어봤다. 최종적으로 브라우저 Network에서 결과값 반환까지 확인 했다!
이해하는데 1시간 반 정도 걸렸고 약간 외국어 지문 읽는 느낌이다.
이제 DAO가 뭔지, Service가 뭔지 조금은 알 것 같다.
새로운 외국어를 배우는 것이라고 친다면
처음에는 모르는 문장을 통채로 읽어봐야 하듯이
그리고 그 문장을 이해하는 과정에서 뇌에 불나고 포기하고 싶지만 끝까지 분해해서 읽으면 첨첨 쉬워지듯이
외국어를 익힌다고 여기고 끈질기게 이해하려는 것이 필요하다.
특히 
#### 꿀팁
- Shift+f6 : 파일 이름 새로고침

# 2026-05-21 수업 32일차

#### json-jdbc 파일 동작 흐름 이해해보기!!! (메모 [등록] 버튼 클릭 했을때) : 1시간 30분 소요
- page.js에서 input onChange가 감지해 useState 함수 setContent를 실행해 텍스트필드 내용을 content로 받는다.
  page.js
- 등록 버튼을 클릭하면 handleSubmit 함수가 동작 한다.
- handleSubmit에서 addMemo(content) 실행한다.
  memoStore.js
- addMemo는 useMemoStroe에 있다.
- addMemo에서 createMemo(content)를 실행해서 savedMomo에 결과를 받아온다.
  memApi.js
- 8080/api/memos으로 서블릿의 doPost를 실행하여 Json 객체를 반환한다.

src>MemoApiServlet.java
- doPost에서 받아온 응답값으로 registMemo(req, resp)를 실행한다.
- registMemo 에서는
- MemoDTO에  mapper.readValue(req.getReader(), MemoDTO.class)로 응답을 받아온 값을 저장한다.
- MemoDTO에서 getContent로 content 값을 꺼내 저장한다.
- content 값이 비었다면 SC_BAD_REQUEST(400)을 반환한다.
- content 값이 비었다면2 F12 -> Network > Previewe or Response Tab에서 확인 가능한 에러 메세지를 반환한다.
- content 값이 들었다면 SC_CREATED(200)을 반환하고,
- MemoServie.java의 registMemo(content)를 실행해 결과값을 savedMemo에 저장한다.
    - src> MemoService.java
    - rigistMemo 함수에서는 getConnetion으로 DB를 연결하고 memoDAO.insertMemo를 실행한다.
        - src > MemoDAO.java > insertMemo
        - INSERT INTO tbl_memo 쿼리 문으로 content를 저장한다.
        - pstmt.getGeneratedKeys()로 자동으로 생성한 번호(Generated Keys)를 받아와 setlectMemoById로 저장한 데이터를 MemoDTO 형식의 객체에 저장해 반환한다.
          -setlectMemoById에서는 id, content,만들어진 시간을 MemoDTO에 저장한다.
- 저장된 savedMemo를 writeValue(resp.getWriter(), savedMemo)를 통해 브라우저에 응답값으로 반환한다.
- 브라우저에서 반환된 내용은 개발자도구 > Network > Response에서 확인 가능하다.


## 프로젝트1
- 서블릿 프로젝트에서 React 사용 
- 리액트 3000번에서 보내서 8080이 받도록 설정
- next.config.mjs 설정 파일 수정
  - 코드
``` 
const nextConfig = {
  //  - URL 재작성 규칙을 정의하는 함수
  //   - localhost:3000/api/memos 로 작성하면 로컬 호스트 8000번으로 바꾸는 규칙 정의
  async rewrites() {
    return [{
      //개발 서버가 감지할 요청 주소. api로 시작하는 모든 요청
      source: '/api/:path*', //api로 시작하는 모든 요청
      //실제 요청을 전달할 대상 주소
      destination: 'http://localhost:8000/api/:path*', 
    }]
  }
};
export default nextConfig;

```
src > api > MemoApi.js 파일 작성
- 브라우저가 자바 서블릿(백엔드) 주소인 /api/memos로 요청을 날려 메모 목록을 받아오거나(GET), 새 메모를 등록하는(POST) 통신 전용 함수
```
// 1. 메모 목록 조회
export async function getMemos(){

    //이전에 작성한 config파일의 rewrite를 사용해 'http://localhost:8000/api/:path*'로 보내 서블릿이 처리할 수 있게 된다.
    const respones = await fetch('/api/memos');

    if(!respones.ok){
        throw new Error('메모 목록을 불러오지 못했습니다.');
        //get 요청 보냄 > 서블릿의 doget 동작 > memo 객체에 든 static List를 반환
    }
    //Javascript 데이터로 변환해서 반환
    //반환값은 Memo 객체들이 들어있는 배열
    return respones.json();
}

// 2. 메모 등록
export async function createMemos(content){
    const response = await fetch('/api/memos', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        //JSON 문자열로 바꾼다.
        body: JSON.stringify( {content} ),
    });

    if(!response.ok){
        throw new Error('메모 등록 실패');
    }
    //서버가 저장 후 돌려준 메모 객체 반환
    //{id: 3, content: '새 메모'}
    return response.json();
}
```
src > store > memoStore.js 파일 작성
- MemoApi가 백엔드에서 털어온 메모 데이터 배열을 보관해 두고, 리액트 화면 어디서든 로딩 상태, 에러 메시지, 메모 목록을 편하게 꺼내 쓰도록 관리해 주는 '중앙 데이터 저장소(Zustand)' 작성
```
//frontend 폴더에 zustand 설치 : npm install zustand
//zustand store 만들고 초기 값 지정
export const useMemoStore = create((set) =>({
    memos: [],
    loading: false,
    error:'',
    //서버에서 메모 목록을 가져오는 action
    fetchMemos: async() => {
        //요청 시작 전 loading은 true로, 이전 에러 메시지는 빈 문자열로 초기화
        set({loading: true, error: ''});

        try{
            // memos 빈 배열에  MemoApi의 함수 호출 통해 가져온 메모 데이터 배열을 저장함
            //받아온 배열을 zustand 상태에 저장
            const memos = await getMemos();
            //축약 표현  memos : memos
            set( {memos} );
        } catch (error) {
            //요청중에 문제가 생겼을 시 에러 메세지를 상태에 저장
            set( {error: error.message} )
        } finally {
            set( {loading: false} );
        }
        },
        //메모 등록 action
        addMemo : async (content)=> {
            set( {error: ''} );
            try{
                const savedMemo = await createMemos(content);
                set ( (state) => ({
                    //기존 배열을 스프레드 문법으로 복사해 새로운 배열에 할당한다.
                    //React가 새로운 배열임을 인식할수 있게 함.
                    memos: [...state.memo, savedMemo],
                }));
            }catch(error){
                set ( {error: error.message} );
            }
        },
}))
```
app > page.js 작성
```
//이 파일을 클라이언트 컴포넌트로 만든다고 선언
'use client'
import {useEffect, useState} from "react";
import {useMemoStore} from "../store/memoStore";

export default function Home() {

    const [content, setContent] = useState('');

    const {memos, loading, error, fetchMemos, addMemo} = useMemoStore();

    //컴포넌트가 처음 화면에 나타났을 때 메모 목록을 불러온다.
    //처음 마운트 됐을때 상태 가져오는 hook
    // fetchMemos가 변경 될 때만 useEffect 실행
    useEffect(() => {
        fetchMemos();
    }, [fetchMemos]);

    const handleSubmit = async (e) => {
        e.preventDefault(); //form 기본 새로고침 동작 막기

        //입력값이 비어있거나 공백이면 서버에 보내지 않는다.
        if (!content.trim()) {
            return;
        }

        //Zustand의 action을 호출하여 서버에 메모 등록 요청을 보냄
        await addMemo(content);

        setContent('');

    }

    return (
        <main>
            <h1>Memo API</h1>
            <form onSubmit={handleSubmit}>
                <input
                    type="text"
                    value={content}
                    onChange={(e) => setContent(e.target.value)}
                    placeholder="메모 내용 입력하세요."
                />
                <button type="submit">등록</button>
            </form>

           {/*&&로 loading false거나, error가 falsely(''값)일 떄*/}
          {loading && <p>불러오는중...</p>}

          {error && <p>{error}</p>}

          <ul>
            {memos.map((memo)=> (
              <li key={memo.id}> {memo.content} </li>
            ))}
          </ul>

        </main>
    )
}


```
- 흐름 : client -> server[Json -> dto ->list ->Json] 

## 프로젝트2
#### 역할 분리
- 프로젝트1: Next > Servelet > Static List > JSON 응답
- 프로젝트2: Next > Servelet > servcie > DAO > JDBC > MYSQL > JSON응답

- memo_DB 생성 및 데이터 insert
- build.gradle에 설정
- resources > db.properties 작성
- common > JDBCTemplate 작성
- 나머지 파일 작성... 

#### 흐름 정리 (메모 [등록] 버튼 클릭 했을때) 
- page.js에서 input onChange가 감지해 useState 함수 setContent를 실행해 텍스트필드 내용을 content로 받는다.
page.js
- 등록 버튼을 클릭하면 handleSubmit 함수가 동작 한다.
- handleSubmit에서 addMemo(content) 실행한다.
memoStore.js
- addMemo는 useMemoStroe에 있다.
- addMemo에서 createMemo(content)를 실행해서 savedMomo에 결과를 받아온다.
memApi.js
- 8080/api/memos으로 서블릿의 doPost를 실행하여 Json 객체를 반환한다.

src>MemoApiServlet.java
- doPost에서 받아온 응답값으로 registMemo(req, resp)를 실행한다.
- registMemo 에서는
- MemoDTO에  mapper.readValue(req.getReader(), MemoDTO.class)로 응답을 받아온 값을 저장한다.
- MemoDTO에서 getContent로 content 값을 꺼내 저장한다.
- content 값이 비었다면 SC_BAD_REQUEST(400)을 반환한다.
- content 값이 비었다면2 F12 -> Network > Previewe or Response Tab에서 확인 가능한 에러 메세지를 반환한다.
- content 값이 들었다면 SC_CREATED(200)을 반환하고, 
- MemoServie.java의 registMemo(content)를 실행해 결과값을 savedMemo에 저장한다.
   - src> MemoService.java
   - rigistMemo 함수에서는 getConnetion으로 DB를 연결하고 memoDAO.insertMemo를 실행한다.
        - src > MemoDAO.java > insertMemo
        - INSERT INTO tbl_memo 쿼리 문으로 content를 저장한다.
        - pstmt.getGeneratedKeys()로 자동으로 생성한 번호(Generated Keys)를 받아와 setlectMemoById로 저장한 데이터를 MemoDTO 형식의 객체에 저장해 반환한다.
        -setlectMemoById에서는 id, content,만들어진 시간을 MemoDTO에 저장한다.
- 저장된 savedMemo를 writeValue(resp.getWriter(), savedMemo)를 통해 브라우저에 응답값으로 반환한다.
- 브라우저에서 반환된 내용은 개발자도구 > Network > Response에서 확인 가능하다.

Git 문의 메모
```
Contributor name remains after rewriting commit history
Contributor listings are derived from repository history and cached data, and they can’t be manually edited or removed by Support.

The Contributors section on a repository is generated automatically by GitHub based on commit history and repository metadata. It isn’t something GitHub Support can manually edit or remove for a specific user.

Based on GitHub’s documentation, here’s what’s important to know:

Rewriting history with tools like git-filter-repo removes commits from the repository, but contributor data may continue to appear due to how GitHub calculates and caches contributor statistics.
There is no supported way to manually remove a single contributor from the Contributors list.
The list updates only when GitHub reprocesses repository data, and in some cases a username may remain visible even after commits are removed.
If the commits from JUHYE0925 are fully removed from all branches and refs (and you’ve force‑pushed the cleaned history), there’s nothing further you can do from the repository side to directly control the Contributors display.

If this situation is blocking you or you believe the contributor is shown despite no remaining references in the repository, the correct next step is to submit this ticket to GitHub Support. Support can review whether the contributor data can be recalculated or cleared on the backend, which isn’t something that can be done through the UI or Git commands.

You’ve already taken the right technical steps by filtering the history. Proceeding with the support request is the appropriate path from here.
```