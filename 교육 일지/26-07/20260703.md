**오늘의 한 줄 평:** 프로젝트 진행

#### 꿀팁 
# 2026-07-03 수업 60일차

----
# 수업(개발) 일지

**작업 항목:** 점수이력(`score_histories`) 테이블 내 재료 `category_id` 추가에 따른 반영 (PR #188, refactor)
**작업 영역:** 냉파 점수 / 통계 도메인

---

## 1. 오늘 한 일

- `score_histories` 테이블에 재료의 `category_id`를 추가하는 리팩토링 작업 진행
- 기존에는 점수 이력에 테이블에는 카테고리 id 정보가 없어, 점수 상세 이력 및 만료 상세 이력 내 상세 이력에 앞에 표시되는 재료별 이모지를 표시 할 수 없음.
- 따라서 `score_histories` 테이블에 `category_id`를 ALTER TABELS 쿼리를 통해 추가하고, 관련 코드를 수정
- 수정 시에 init.sql에도 반영함