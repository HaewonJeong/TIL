**오늘의 한 줄 평:** 프로젝트 진행

#### 꿀팁 
# 2026-07-02 수업 62일차

----
### 진행 사항
- AWS EC2를 이용한 백앤드 배포
- Git actions를 이용한 배포 자동화
- Vercel을 이용한 프론트 엔드 배포

AWS
Elastic Beanstalk 생성
NaengpaMaster - 애플리케이션
NaengpaMaster-env - 환경
서비스 역할 - Elastic bean talk
인스턴스 EC2 역할 준다  역할은 노션에
엘라스틱 빈스 톡 더 큼 -> 그안에 EC2
키 페어 - EC2에 접속할때 필요한 접속키
DB 삭제 정책 - 스냅샷

도커, WSL 설치
인텔리 제이 -
Dockerfile - docker build -t wony77/backend .할때 필요한 것. 도커파일이 있는 곳에서 명령어 필요. 도커 이미지 생성. / 깃 액션에서 생성해줌.

ECR
- 도커 이미지 저장소
- 생성

Git > Settings > Secret and variables > Actions > Repository secrets 에 입력
- env에 수정사항이 있으면 secert 파일 수정 필요
- yml에 secret 입력 입력
- Environment secrets - 나도 못봄. 한번 넣으면 수정 못함
- Environment variables - 보인다. 봐도 되는
```
AWS_ACCESS_KEY_ID
AWS_REGION
AWS_SECRET_ACCESS_KEY
EB_APP_NAME
EB_ENV_NAME
```

IAM 사용자 - 보안 자격증명 -엑세스 키 - 다운로드
* secret에 넣으면 못보기 때문에 파일 가지고 있어야함.  - 나중에 공유

NaengpaMaster-env > 구성 > 업데이트, 모니터링 및 로깅
- 나머지 입력 env 입력. plan text

### vercel 배포 후 https가 필수이나 백앤드 서버 주소는 http인 문제 해결
- Vercel > Environment Variables > BACKEND_URL > http://주소 입력 > redeployment
- vercel에 환경변수로 BACKEND_URL이라는 변수를 사용해서 Route Handler를 적용 위해 Frontent 코드 수정
```
❯ 현재 코드에서 vercel에 환경변수로 BACKEND_URL이라는 변수를 사용해서 Route Handler를 적용하려고해. 소스를 어디를 수정봐야하는지 확인해봐                     
                                                                                                                                                              
  Searched for 4 patterns, read 6 files, ran 5 shell commands                                                                                                 
                                                                                                                                                              
● 조사해보니 이 프로젝트는 SPA 형태로, 브라우저에서 axios가 NEXT_PUBLIC_API_BASE_URL을 통해 백엔드로 직접 호출하는 구조입니다. Route Handler로 프록시하려면   
  아래 파일들을 손봐야 합니다.                                    Jump to bottom (ctrl+End) ↓                                                       

```
